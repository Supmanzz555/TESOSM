# File Specification: https://gist.github.com/Shadows-of-Fire/88ac714a758636c57a52e32ace5474c1

# Apotheosis Balancer - Attribute Multipliers

multiplier {
    # Multiplier for generic.max_health attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.max_health"=1.0

    # Multiplier for generic.follow_range attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.follow_range"=1.0

    # Multiplier for generic.knockback_resistance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.knockback_resistance"=1.0

    # Multiplier for generic.movement_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.movement_speed"=1.0

    # Multiplier for generic.flying_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.flying_speed"=1.0

    # Multiplier for generic.attack_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.attack_damage"=1.0

    # Multiplier for generic.attack_knockback attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.attack_knockback"=1.0

    # Multiplier for generic.attack_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.attack_speed"=1.0

    # Multiplier for generic.armor attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.armor"=1.0

    # Multiplier for generic.armor_toughness attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.armor_toughness"=1.0

    # Multiplier for generic.luck attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.luck"=1.0

    # Multiplier for zombie.spawn_reinforcements attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:zombie.spawn_reinforcements"=1.0

    # Multiplier for horse.jump_strength attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:horse.jump_strength"=1.0
    S:"more_rpg_classes:damage_reflect_modifier"=1.0
    S:"more_rpg_classes:arcane_fuse_modifier"=1.0
    S:"more_rpg_classes:lifesteal_modifier"=1.0
    S:"more_rpg_classes:rage_modifier"=1.0

    # Multiplier for damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ranged_weapon:damage"=1.0

    # Multiplier for haste attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ranged_weapon:haste"=1.0

    # Multiplier for critical_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"spell_power:critical_damage"=1.0

    # Multiplier for critical_chance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"spell_power:critical_chance"=1.0

    # Multiplier for haste attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"spell_power:haste"=1.0

    # Multiplier for arcane attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"spell_power:arcane"=1.0

    # Multiplier for fire attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"spell_power:fire"=1.0

    # Multiplier for frost attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"spell_power:frost"=1.0

    # Multiplier for healing attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"spell_power:healing"=1.0

    # Multiplier for lightning attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"spell_power:lightning"=1.0

    # Multiplier for soul attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"spell_power:soul"=1.0
    S:"spell_power:earth"=1.0
    S:"spell_power:water"=1.0
    S:"spell_power:air"=1.0

    # Multiplier for resistance.generic attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"spell_power:resistance.generic"=1.0
    S:"alshanex_familiars:sound_magic_resist"=1.0
    S:"alshanex_familiars:sound_spell_power"=1.0

    # Multiplier for armor_pierce attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:armor_pierce"=1.0

    # Multiplier for armor_shred attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:armor_shred"=1.0

    # Multiplier for arrow_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:arrow_damage"=1.0

    # Multiplier for arrow_velocity attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:arrow_velocity"=1.0

    # Multiplier for cold_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:cold_damage"=1.0

    # Multiplier for crit_chance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:crit_chance"=1.0

    # Multiplier for crit_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:crit_damage"=1.0

    # Multiplier for current_hp_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:current_hp_damage"=1.0

    # Multiplier for dodge_chance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:dodge_chance"=1.0

    # Multiplier for draw_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:draw_speed"=1.0

    # Multiplier for experience_gained attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:experience_gained"=1.0

    # Multiplier for fire_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:fire_damage"=1.0

    # Multiplier for ghost_health attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:ghost_health"=1.0

    # Multiplier for healing_received attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:healing_received"=1.0

    # Multiplier for life_steal attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:life_steal"=1.0

    # Multiplier for mining_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:mining_speed"=1.0

    # Multiplier for overheal attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:overheal"=1.0

    # Multiplier for prot_pierce attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:prot_pierce"=1.0

    # Multiplier for prot_shred attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:prot_shred"=1.0

    # Multiplier for elytra_flight attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:elytra_flight"=1.0

    # Multiplier for creative_flight attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:creative_flight"=1.0

    # Multiplier for projectile_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"scguns:projectile_speed"=1.0

    # Multiplier for additional_bullet_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"scguns:additional_bullet_damage"=1.0

    # Multiplier for reload_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"scguns:reload_speed"=1.0

    # Multiplier for bullet_damage_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"scguns:bullet_damage_multiplier"=1.0

    # Multiplier for spread_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"scguns:spread_multiplier"=1.0

    # Multiplier for xray_follow_range attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"enhancedai:xray_follow_range"=1.0

    # Multiplier for push_resistance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"enhancedai:push_resistance"=1.0

    # Multiplier for spawn_reinforcements_chance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"enhancedai:spawn_reinforcements_chance"=1.0

    # Multiplier for fall_flying attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"caelus:fall_flying"=1.0

    # Multiplier for swim_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:swim_speed"=1.0

    # Multiplier for nametag_distance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:nametag_distance"=1.0

    # Multiplier for entity_gravity attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:entity_gravity"=1.0

    # Multiplier for block_reach attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:block_reach"=1.0

    # Multiplier for entity_reach attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:entity_reach"=1.0

    # Multiplier for step_height_addition attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:step_height_addition"=1.0
    S:"block_factorys_bosses:roll_animtime"=1.0
    S:"block_factorys_bosses:movement_recovery"=1.0
    S:"block_factorys_bosses:burning_stability"=1.0

    # Multiplier for ars_nouveau.perk.warding attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.warding"=1.0

    # Multiplier for ars_nouveau.perk.mana_regen attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.mana_regen"=1.0

    # Multiplier for ars_nouveau.perk.max_mana attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.max_mana"=1.0

    # Multiplier for ars_nouveau.perk.spell_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.spell_damage"=1.0

    # Multiplier for ars_nouveau.perk.saturation attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.saturation"=1.0

    # Multiplier for ars_nouveau.perk.wixie attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.wixie"=1.0

    # Multiplier for ars_nouveau.perk.feather attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.feather"=1.0

    # Multiplier for ars_elemental.perk.summon_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_elemental.perk.summon_power"=1.0

    # Multiplier for crit_rate attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2damagetracker:crit_rate"=1.0

    # Multiplier for crit_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2damagetracker:crit_damage"=1.0

    # Multiplier for bow_strength attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2damagetracker:bow_strength"=1.0

    # Multiplier for explosion_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2damagetracker:explosion_damage"=1.0

    # Multiplier for fire_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2damagetracker:fire_damage"=1.0

    # Multiplier for magic_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2damagetracker:magic_damage"=1.0

    # Multiplier for damage_absorption attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2damagetracker:damage_absorption"=1.0

    # Multiplier for damage_reduction attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2damagetracker:damage_reduction"=1.0

    # Multiplier for target_stationary_flying_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"witherstormmod:target_stationary_flying_speed"=1.0

    # Multiplier for slow_flying_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"witherstormmod:slow_flying_speed"=1.0

    # Multiplier for evolution_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"witherstormmod:evolution_speed"=1.0

    # Multiplier for hunchback_follow_range attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"witherstormmod:hunchback_follow_range"=1.0

    # Multiplier for stamina attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:stamina"=1.0

    # Multiplier for magic_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:magic_damage"=1.0

    # Multiplier for melee_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:melee_damage"=1.0

    # Multiplier for ranged_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:ranged_damage"=1.0

    # Multiplier for tamed_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:tamed_damage"=1.0

    # Multiplier for sword_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:sword_damage"=1.0

    # Multiplier for axe_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:axe_damage"=1.0

    # Multiplier for trident_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:trident_damage"=1.0

    # Multiplier for fortune attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:fortune"=1.0

    # Multiplier for healing attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:healing"=1.0

    # Multiplier for jump attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:jump"=1.0

    # Multiplier for resistance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:resistance"=1.0

    # Multiplier for magic_resistance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:magic_resistance"=1.0

    # Multiplier for melee_resistance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:melee_resistance"=1.0

    # Multiplier for ranged_resistance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:ranged_resistance"=1.0

    # Multiplier for mining_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:mining_speed"=1.0

    # Multiplier for pickaxe_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:pickaxe_speed"=1.0

    # Multiplier for axe_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:axe_speed"=1.0

    # Multiplier for shovel_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:shovel_speed"=1.0

    # Multiplier for sprinting_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:sprinting_speed"=1.0

    # Multiplier for knockback attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:knockback"=1.0

    # Multiplier for repair_cost attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:repair_cost"=1.0

    # Multiplier for armor_shred attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:armor_shred"=1.0

    # Multiplier for toughness_shred attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:toughness_shred"=1.0

    # Multiplier for protection_shred attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:protection_shred"=1.0

    # Multiplier for resistance_shred attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:resistance_shred"=1.0

    # Multiplier for tamed_resistance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:tamed_resistance"=1.0

    # Multiplier for magic_resistance_shred attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:magic_resistance_shred"=1.0

    # Multiplier for melee_resistance_shred attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:melee_resistance_shred"=1.0

    # Multiplier for ranged_resistance_shred attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:ranged_resistance_shred"=1.0

    # Multiplier for natural_regeneration attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:natural_regeneration"=1.0

    # Multiplier for stealth attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:stealth"=1.0

    # Multiplier for life_steal attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:life_steal"=1.0

    # Multiplier for fall_reduction attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:fall_reduction"=1.0

    # Multiplier for bow_projectile_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:bow_projectile_speed"=1.0

    # Multiplier for crossbow_projectile_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:crossbow_projectile_speed"=1.0

    # Multiplier for experience attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:experience"=1.0

    # Multiplier for critical_hit attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"obscure_api:critical_hit"=1.0

    # Multiplier for critical_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"obscure_api:critical_damage"=1.0

    # Multiplier for dodge attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"obscure_api:dodge"=1.0

    # Multiplier for parry attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"obscure_api:parry"=1.0

    # Multiplier for magic_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"obscure_api:magic_damage"=1.0

    # Multiplier for magic_resistance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"obscure_api:magic_resistance"=1.0

    # Multiplier for penetration attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"obscure_api:penetration"=1.0

    # Multiplier for regeneration attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"obscure_api:regeneration"=1.0

    # Multiplier for healing_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"obscure_api:healing_power"=1.0

    # Multiplier for accuracy attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"obscure_api:accuracy"=1.0

    # Multiplier for resilience attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"obscure_api:resilience"=1.0
    S:"tconstruct:generic.bouncy"=1.0
    S:"tconstruct:player.use_item_speed"=1.0
    S:"tconstruct:generic.protection_cap"=1.0
    S:"tconstruct:player.critical_damage"=1.0
    S:"tconstruct:generic.jump_boost"=1.0
    S:"tconstruct:generic.safe_fall_distance"=1.0
    S:"tconstruct:player.jump_count"=1.0
    S:"tconstruct:generic.knockback_multiplier"=1.0
    S:"tconstruct:player.mining_speed_multiplier"=1.0
    S:"tconstruct:player.experience_multiplier"=1.0
    S:"tconstruct:generic.crouch_damage_multiplier"=1.0
    S:"tconstruct:generic.good_effect_duration_multiplier"=1.0
    S:"tconstruct:generic.bad_effect_duration_multiplier"=1.0
    S:"ticex:healing_received"=1.0
    S:"ticex:damage_taken"=1.0

    # Multiplier for max_mana attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:max_mana"=1.0

    # Multiplier for mana_regen attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:mana_regen"=1.0

    # Multiplier for cooldown_reduction attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:cooldown_reduction"=1.0

    # Multiplier for spell_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:spell_power"=1.0

    # Multiplier for spell_resist attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:spell_resist"=1.0

    # Multiplier for cast_time_reduction attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:cast_time_reduction"=1.0

    # Multiplier for summon_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:summon_damage"=1.0

    # Multiplier for fire_magic_resist attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:fire_magic_resist"=1.0

    # Multiplier for ice_magic_resist attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:ice_magic_resist"=1.0

    # Multiplier for lightning_magic_resist attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:lightning_magic_resist"=1.0

    # Multiplier for holy_magic_resist attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:holy_magic_resist"=1.0

    # Multiplier for ender_magic_resist attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:ender_magic_resist"=1.0

    # Multiplier for blood_magic_resist attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:blood_magic_resist"=1.0

    # Multiplier for evocation_magic_resist attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:evocation_magic_resist"=1.0

    # Multiplier for nature_magic_resist attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:nature_magic_resist"=1.0

    # Multiplier for eldritch_magic_resist attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:eldritch_magic_resist"=1.0

    # Multiplier for fire_spell_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:fire_spell_power"=1.0

    # Multiplier for ice_spell_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:ice_spell_power"=1.0

    # Multiplier for lightning_spell_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:lightning_spell_power"=1.0

    # Multiplier for holy_spell_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:holy_spell_power"=1.0

    # Multiplier for ender_spell_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:ender_spell_power"=1.0

    # Multiplier for blood_spell_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:blood_spell_power"=1.0

    # Multiplier for evocation_spell_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:evocation_spell_power"=1.0

    # Multiplier for nature_spell_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:nature_spell_power"=1.0

    # Multiplier for eldritch_spell_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:eldritch_spell_power"=1.0

    # Multiplier for max_health_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"fallen_gems_affixes:max_health_damage"=1.0

    # Multiplier for extra_difficulty attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2hostility:extra_difficulty"=1.0

    # Multiplier for extra_scale attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2hostility:extra_scale"=1.0
    S:"legendarysurvivaloverhaul:heating_temperature"=1.0
    S:"legendarysurvivaloverhaul:cooling_temperature"=1.0
    S:"legendarysurvivaloverhaul:heat_resistance"=1.0
    S:"legendarysurvivaloverhaul:cold_resistance"=1.0
    S:"legendarysurvivaloverhaul:thermal_resistance"=1.0
    S:"legendarysurvivaloverhaul:body_resistance"=1.0
    S:"legendarysurvivaloverhaul:head_resistance"=1.0
    S:"legendarysurvivaloverhaul:chest_resistance"=1.0
    S:"legendarysurvivaloverhaul:right_arm_resistance"=1.0
    S:"legendarysurvivaloverhaul:left_arm_resistance"=1.0
    S:"legendarysurvivaloverhaul:legs_resistance"=1.0
    S:"legendarysurvivaloverhaul:feet_resistance"=1.0
    S:"legendarysurvivaloverhaul:broken_heart"=1.0
    S:"legendarysurvivaloverhaul:permanent_heart"=1.0
    S:"legendarysurvivaloverhaul:broken_heart_resilience"=1.0
    S:"constructs_casting:slime_spell_power"=1.0
    S:"constructs_casting:slime_magic_resist"=1.0
    S:"doggytalents:generic.jump_power"=1.0
    S:"doggytalents:generic.crit_chance"=1.0
    S:"doggytalents:generic.crit_bonus"=1.0

    # Multiplier for golem_regen attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"modulargolems:golem_regen"=1.0

    # Multiplier for golem_sweep attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"modulargolems:golem_sweep"=1.0

    # Multiplier for golem_size attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"modulargolems:golem_size"=1.0

    # Multiplier for golem_jump attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"modulargolems:golem_jump"=1.0
    S:"traveloptics:aqua_magic_resist"=1.0
    S:"traveloptics:aqua_spell_power"=1.0

    # Multiplier for spell_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gtbcs_spell_lib:spell_damage"=1.0

    # Multiplier for magic_projectile_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gtbcs_spell_lib:magic_projectile_damage"=1.0

    # Multiplier for spell_crit_chance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gtbcs_spell_lib:spell_crit_chance"=1.0

    # Multiplier for spell_crit_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gtbcs_spell_lib:spell_crit_damage"=1.0
    S:"cataclysm_spellbooks:abyssal_magic_resist"=1.0
    S:"cataclysm_spellbooks:abyssal_spell_power"=1.0
    S:"cataclysm_spellbooks:technomancy_magic_resist"=1.0
    S:"cataclysm_spellbooks:technomancy_spell_power"=1.0
    S:"enigmaticdice:size_scale"=1.0
    S:"animus:unarmed_damage"=1.0

    # Multiplier for tacz.bullet_resistance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"tacz:tacz.bullet_resistance"=1.0

    # Multiplier for bullet_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:bullet_damage"=1.0

    # Multiplier for armor_piercing_ratio attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:armor_piercing_ratio"=1.0

    # Multiplier for headshot_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:headshot_multiplier"=1.0

    # Multiplier for bullet_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:bullet_speed"=1.0

    # Multiplier for horz_recoil attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:horz_recoil"=1.0

    # Multiplier for vert_recoil attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:vert_recoil"=1.0

    # Multiplier for rpm attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:rpm"=1.0

    # Multiplier for ammo_capacity attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:ammo_capacity"=1.0

    # Multiplier for reload_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:reload_speed"=1.0

    # Multiplier for internal_do_not_use_0 attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:internal_do_not_use_0"=1.0

    # Multiplier for aim_lock_range attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:aim_lock_range"=1.0

    # Multiplier for aim_lock_angle attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"gunsmithlib:aim_lock_angle"=1.0

    # Multiplier for posture attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"apotheosis_modern_ragnarok:posture"=1.0

    # Multiplier for dig_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"quality_equipment:dig_speed"=1.0

    # Multiplier for projectile_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"quality_equipment:projectile_damage"=1.0

    # Multiplier for jump_height attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"quality_equipment:jump_height"=1.0

    # Multiplier for breaking_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:breaking_speed"=1.0
    S:"projectile_damage:generic"=1.0
    S:"soulsweapons:posture_buildup_resistance"=1.0
    S:"soulsweapons:base_posture_increase"=1.0
    S:"soulsweapons:bleed_buildup_resistance"=1.0
    S:"soulsweapons:bleed_damage_resistance"=1.0

    # Multiplier for casting_movespeed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"irons_spellbooks:casting_movespeed"=1.0

    # Multiplier for mount_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:mount_speed"=1.0

    # Multiplier for consuming_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:consuming_speed"=1.0

    # Multiplier for damage_reflection attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"puffish_attributes:damage_reflection"=1.0
    S:"manaunification:max_mana"=1.0
    S:"manaunification:mana_regen"=1.0
    S:"too_many_bows:bow_draw_speed"=1.0
    S:"too_many_bows:bow_damage"=1.0
    S:"too_many_bows:bow_crit_chance"=1.0
    S:"block_factorys_bosses:roll_count"=1.0
    S:"animus:bonus_sacrifice"=1.0
    S:"animus:bonus_self_sacrifice"=1.0
    S:"animus:bonus_demon_will"=1.0
    S:"animus:sigil_cost_reduction"=1.0

    # Multiplier for shield_defense attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2weaponry:shield_defense"=1.0

    # Multiplier for reflect_time attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"l2weaponry:reflect_time"=1.0

    # Multiplier for mount_max_health attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"tyzs_skills:mount_max_health"=1.0
    S:"extraspellattributes:reabsorption"=1.0
    S:"extraspellattributes:converttofire"=1.0
    S:"extraspellattributes:convertfromfire"=1.0
    S:"extraspellattributes:converttoarcane"=1.0
    S:"extraspellattributes:convertfromarcane"=1.0
    S:"extraspellattributes:converttofrost"=1.0
    S:"extraspellattributes:convertfromfrost"=1.0
    S:"extraspellattributes:converttoheal"=1.0
    S:"extraspellattributes:glancingblow"=1.0
    S:"extraspellattributes:spellsuppression"=1.0
    S:"extraspellattributes:serenity"=1.0
    S:"extraspellattributes:endurance"=1.0
    S:"extraspellattributes:defiance"=1.0
    S:"extraspellattributes:determination"=1.0

    # Multiplier for distance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"combatroll:distance"=1.0

    # Multiplier for recharge attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"combatroll:recharge"=1.0

    # Multiplier for count attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"combatroll:count"=1.0

    # Multiplier for max_stamina attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:max_stamina"=1.0

    # Multiplier for slow_climb_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:slow_climb_speed"=1.0

    # Multiplier for stamina_regen attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:stamina_regen"=1.0

    # Multiplier for current_stamina attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:current_stamina"=1.0

    # Multiplier for exhaustion_duration_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:exhaustion_duration_multiplier"=1.0

    # Multiplier for regen_delay_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:regen_delay_multiplier"=1.0

    # Multiplier for stamina_action_recovery attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:stamina_action_recovery"=1.0

    # Multiplier for penalty_gain_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:penalty_gain_multiplier"=1.0

    # Multiplier for penalty_decay_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:penalty_decay_multiplier"=1.0

    # Multiplier for penalty_amount_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:penalty_amount_multiplier"=1.0

    # Multiplier for sprint_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:sprint_speed"=1.0

    # Multiplier for current_weight attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:current_weight"=1.0

    # Multiplier for weight_limit attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:weight_limit"=1.0

    # Multiplier for weight_calculation_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:weight_calculation_multiplier"=1.0

    # Multiplier for stamina_usage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:stamina_usage"=1.0

    # Multiplier for jump_cost_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:jump_cost_multiplier"=1.0

    # Multiplier for sprint_cost_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:sprint_cost_multiplier"=1.0

    # Multiplier for attack_cost_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:attack_cost_multiplier"=1.0

    # Multiplier for missed_attack_cost_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:missed_attack_cost_multiplier"=1.0

    # Multiplier for block_cost_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:block_cost_multiplier"=1.0

    # Multiplier for use_item_cost_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:use_item_cost_multiplier"=1.0

    # Multiplier for block_break_cost_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:block_break_cost_multiplier"=1.0

    # Multiplier for swim_cost_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:swim_cost_multiplier"=1.0

    # Multiplier for climb_cost_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:climb_cost_multiplier"=1.0

    # Multiplier for block_place_cost_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:block_place_cost_multiplier"=1.0

    # Multiplier for elytra_cost_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:elytra_cost_multiplier"=1.0

    # Multiplier for bonus_stamina_capacity attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:bonus_stamina_capacity"=1.0

    # Multiplier for bonus_stamina_decay_rate attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:bonus_stamina_decay_rate"=1.0

    # Multiplier for bonus_stamina_decay_delay attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:bonus_stamina_decay_delay"=1.0

    # Multiplier for excess_conversion_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"peakstamina:excess_conversion_multiplier"=1.0

    # Multiplier for spell_potency attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:spell_potency"=1.0

    # Multiplier for spell_duration attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:spell_duration"=1.0

    # Multiplier for spell_range attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:spell_range"=1.0

    # Multiplier for spell_radius attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:spell_radius"=1.0

    # Multiplier for spell_burning attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:spell_burning"=1.0

    # Multiplier for spell_velocity attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:spell_velocity"=1.0

    # Multiplier for casting_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:casting_speed"=1.0

    # Multiplier for cooldown_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:cooldown_discount"=1.0

    # Multiplier for soul_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:soul_discount"=1.0

    # Multiplier for abyss_potency attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:abyss_potency"=1.0

    # Multiplier for frost_potency attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:frost_potency"=1.0

    # Multiplier for geomancy_potency attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:geomancy_potency"=1.0

    # Multiplier for necromancy_potency attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:necromancy_potency"=1.0

    # Multiplier for nether_potency attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:nether_potency"=1.0

    # Multiplier for storm_potency attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:storm_potency"=1.0

    # Multiplier for void_potency attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:void_potency"=1.0

    # Multiplier for wild_potency attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:wild_potency"=1.0

    # Multiplier for wind_potency attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:wind_potency"=1.0

    # Multiplier for abyss_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:abyss_discount"=1.0

    # Multiplier for frost_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:frost_discount"=1.0

    # Multiplier for geomancy_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:geomancy_discount"=1.0

    # Multiplier for necromancy_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:necromancy_discount"=1.0

    # Multiplier for nether_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:nether_discount"=1.0

    # Multiplier for storm_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:storm_discount"=1.0

    # Multiplier for void_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:void_discount"=1.0

    # Multiplier for wild_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:wild_discount"=1.0

    # Multiplier for wind_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"goety:wind_discount"=1.0

    # Multiplier for additional_critical_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"cataclysm:additional_critical_damage"=1.0

    # Multiplier for nature_heal attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"cataclysm:nature_heal"=1.0

    # Multiplier for eat_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"cataclysm:eat_speed"=1.0

    # Multiplier for charge_time attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"cataclysm:charge_time"=1.0

    # Multiplier for line_strength attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"stardew_fishing:line_strength"=1.0

    # Multiplier for bar_size attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"stardew_fishing:bar_size"=1.0

    # Multiplier for treasure_chance_bonus attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"stardew_fishing:treasure_chance_bonus"=1.0

    # Multiplier for golden_chest_bonus attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"stardew_fishing:golden_chest_bonus"=1.0

    # Multiplier for exp_multiplier attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"stardew_fishing:exp_multiplier"=1.0
}


